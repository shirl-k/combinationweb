{{>layouts/header}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Title</th>
        <th scope="col">Content</th>

    </tr>
    </thead>
    <tbody>

    {{#boxingList}}
        <tr>
            <th>{{id}}</th>
            <td><a href="/boxing/{{id}}">{{title}}</a></td>
            <td>{{content}}</td>
        </tr>
    {{/boxingList}}
    </tbody>
</table>

<a href="boxing/new">New Boxing</a>

{{>layouts/footer}}